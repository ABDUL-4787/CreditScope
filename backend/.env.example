# Database connection URL. Default to local SQLite database.
DATABASE_URL=sqlite:///creditscope.db

# Comma-separated list of origins allowed by CORS
CORS_ORIGINS=http://localhost:3000

# API key for the Gemini underwriting analyst (Optional fallback is automatically used if empty)
LLM_API_KEY=
